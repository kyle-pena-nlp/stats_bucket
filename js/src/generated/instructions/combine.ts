/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CombineInstructionAccounts = {
  /** payer */
  payer?: Signer;
  /** source bucket */
  sourceBucket: PublicKey | Pda;
  /** target bucket */
  targetBucket: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CombineInstructionData = {
  discriminator: number;
  sourceName: string;
  targetName: string;
};

export type CombineInstructionDataArgs = {
  sourceName: string;
  targetName: string;
};

export function getCombineInstructionDataSerializer(): Serializer<
  CombineInstructionDataArgs,
  CombineInstructionData
> {
  return mapSerializer<CombineInstructionDataArgs, any, CombineInstructionData>(
    struct<CombineInstructionData>(
      [
        ['discriminator', u8()],
        ['sourceName', string()],
        ['targetName', string()],
      ],
      { description: 'CombineInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 1 })
  ) as Serializer<CombineInstructionDataArgs, CombineInstructionData>;
}

// Args.
export type CombineInstructionArgs = CombineInstructionDataArgs;

// Instruction.
export function combine(
  context: Pick<Context, 'payer' | 'programs'>,
  input: CombineInstructionAccounts & CombineInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'statsBucket',
    'C7DVvsaSQ1k7XcUXoAh9gZyGs6Ki9Qg9zpriBbrcx6tm'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: false as boolean,
      value: input.payer ?? null,
    },
    sourceBucket: {
      index: 1,
      isWritable: false as boolean,
      value: input.sourceBucket ?? null,
    },
    targetBucket: {
      index: 2,
      isWritable: true as boolean,
      value: input.targetBucket ?? null,
    },
    systemProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CombineInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCombineInstructionDataSerializer().serialize(
    resolvedArgs as CombineInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
