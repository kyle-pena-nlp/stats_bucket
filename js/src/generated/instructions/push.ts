/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  f32,
  mapSerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type PushInstructionAccounts = {
  /** payer */
  payer?: Signer;
  /** bucket */
  bucket: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type PushInstructionData = {
  discriminator: number;
  name: string;
  ys: Array<number>;
};

export type PushInstructionDataArgs = { name: string; ys: Array<number> };

export function getPushInstructionDataSerializer(): Serializer<
  PushInstructionDataArgs,
  PushInstructionData
> {
  return mapSerializer<PushInstructionDataArgs, any, PushInstructionData>(
    struct<PushInstructionData>(
      [
        ['discriminator', u8()],
        ['name', string()],
        ['ys', array(f32())],
      ],
      { description: 'PushInstructionData' }
    ),
    (value) => ({ ...value, discriminator: 0 })
  ) as Serializer<PushInstructionDataArgs, PushInstructionData>;
}

// Args.
export type PushInstructionArgs = PushInstructionDataArgs;

// Instruction.
export function push(
  context: Pick<Context, 'payer' | 'programs'>,
  input: PushInstructionAccounts & PushInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'statsBucket',
    'C7DVvsaSQ1k7XcUXoAh9gZyGs6Ki9Qg9zpriBbrcx6tm'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: false as boolean,
      value: input.payer ?? null,
    },
    bucket: {
      index: 1,
      isWritable: true as boolean,
      value: input.bucket ?? null,
    },
    systemProgram: {
      index: 2,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: PushInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getPushInstructionDataSerializer().serialize(
    resolvedArgs as PushInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
